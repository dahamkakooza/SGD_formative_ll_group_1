# Packages not pre-installed on Google Colab — install these manually or via the notebook setup cell.
# Pre-installed Colab packages (pandas, numpy, scikit-learn, matplotlib, seaborn, joblib, tqdm)
# are intentionally unpinned to avoid binary incompatibility with Colab's compiled dependencies.

sentence-transformers
gensim
nltk
beautifulsoup4
lxml
